import { SortByKeywordPipe } from './sort-by-keyword.pipe';

describe('SortByKeywordPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByKeywordPipe();
    expect(pipe).toBeTruthy();
  });
});
