import { ColoredItemFilterDirective } from './colored-item-filter.directive';

describe('ColoredItemFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoredItemFilterDirective();
    expect(directive).toBeTruthy();
  });
});
