import { BaseStyleDirective } from './base-style.directive';

describe('BaseStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseStyleDirective();
    expect(directive).toBeTruthy();
  });
});
